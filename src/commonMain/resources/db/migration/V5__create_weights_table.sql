CREATE TABLE IF NOT EXISTS weights( id INT AUTO_INCREMENT PRIMARY KEY, value FLOAT NOT NULL, date DATETIME(6) NOT NULL, user_id INT NOT NULL, CONSTRAINT FK_WEIGHTS_USER_ID_ID FOREIGN KEY(user_id) REFERENCES users(id) ON DELETE CASCADE ON UPDATE RESTRICT)